#include <secrets.h>

// Copy this file to src/secrets.cpp and replace the placeholders.
// secrets.cpp is ignored by Git and may contain real credentials.
const char *wifiSsid = "YOUR_WIFI_SSID";
const char *wifiPassword = "YOUR_WIFI_PASSWORD";
